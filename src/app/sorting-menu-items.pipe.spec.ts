import { SortingMenuItemsPipe } from './sorting-menu-items.pipe';

describe('SortingMenuItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortingMenuItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
